#include <iostream>
#include <vector>

using namespace std;

vector<int> sieve(int n){
    vector<int> 
}

int main(){
    ios::sync_with_stdio(0); cin.tie(0);

    int n, t;
    vector<int> nums;

    cin >> n;
    while(n--){
        cin >> t;
        nums.push_back(t);
    }
}