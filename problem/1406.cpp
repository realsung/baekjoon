#include <iostream>

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    string code, command, arg;
    int n, m;


    cin >> code;
    cin >> m;
    for(int i=0; i<m; i++){
        cin >> command;
    }
}